#include <iostream>

using namespace std;
int main()
{
    int i;
    
    cout << "加密請按1 解密請按2\n";
    cin >> i;
    
    if(i == 1) {
      int a, s;
      cout << "密碼多少字";
      cin >> a;
      
      cout << "位移多少";
      cin >> s;
      
      char password[a + 1];
      cout << "交出你的密碼";
      cin >> password;
      
      for(int i = 0; i< a; i++){
        password[a] = password[a + 1] + s;
      }
    }
        
    
    return 0;
}
